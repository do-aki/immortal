// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("immortal", "for immortal support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("immortal", "enable immortal support", "no");

if (PHP_IMMORTAL != "no") {
	EXTENSION("immortal", "immortal.c");
}

